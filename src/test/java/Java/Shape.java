package Java;

abstract class Shape 
{
	/*AbstractClass"
	 * Aclass that is declared as "abstract" is know as Abstract class
	 * --- it needs to be extended
	 * Its methods needs to be implemented
	 * Abstract class can not be instantiated
	 * 
	 * Abstract Mathods;
	 * A Method that is decleared as abstract and does not have implementation is known as abstract method
	 * Part of an abstract class
	 * Must be implement by a concrect subclass
	 * Each concrete subclass can implement the method differently
	 */
	abstract void draw();
	

}
